#pragma once
#include <SDL2/SDL.h>

struct Vector2
{
  float x;
  float y;
};

class Game
{
public:
  
  // Constracter
  Game();
  
  // Initializer
  bool Initialize();
  
  // Game Looper
  void RunLoop();
  
  // ShutDown Game
  void Shutdown();
  
private:
  // Helper functions for RunLoop()
  void ProcessInput();
  void UpdateGame();
  void GenerateOutput();
  
  // the pointer of Window generated by SDL
  SDL_Window *mWindow;
  SDL_Renderer *mRenderer; // graphic organizer
  
  // continue flag
  bool mlsRunning;
  // elapsed time from SDL_init
  Uint32 mTicksCount;
	// position vector of Paddle and ball
	Vector2 mPaddlePos;
	Vector2 mBallPos;
	
	// ball velocity vector
	Vector2 mBallVel;

	// paddle difference organizer
	int mPaddleDir;

  // screen
  //const float width = 1024.0f;
	//const float height = 756.0f;
	//const float upperLeftX = 100.0f;
	//const float upperLeftY = 100.0f;
};
